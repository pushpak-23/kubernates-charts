apiVersion: storage.k8s.io/v1
kind: StorageClass

metadata:
  name: solr-ssd-xfs

provisioner: cinder.csi.openstack.org

allowVolumeExpansion: true

reclaimPolicy: Retain

volumeBindingMode: WaitForFirstConsumer

parameters:
  type: rbd-production
  fsType: xfs
  availability: nova_production
